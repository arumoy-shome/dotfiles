#!/usr/bin/env bash

z() {
  local PROJECT="$(basename "$PWD")"

  if [[ -r "$NOTESDIR/$PROJECT/inbox.md" ]]; then
    $EDITOR "$NOTESDIR/$PROJECT/inbox.md"
  else
    $EDITOR "$NOTESDIR/inbox.md"
  fi
}

z "$@"
