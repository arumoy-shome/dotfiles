#!/usr/bin/env bash

# g: helpful wrapper around git.

# Usage: g [OPT] [FILE]
# No arguments: `git status`
# With arguments: acts like `git`

if [[ "$#" -gt 0 ]]; then
  git "$@"
else
  git status
fi

complete -F _command g
