#!/usr/bin/env fish
source env

function install
    brew install \
    git git-lfs hub diff-so-fancy git-standup \
    git-extras git-flow-avh
end

function link
    _mklink ignore $XDG_CONFIG_HOME/git
    _mklink config $XDG_CONFIG_HOME/git
    _mklink message $XDG_CONFIG_HOME/git
end
