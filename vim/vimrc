if &loadplugins
  if has('packages')
    packadd! vim-rsi
    packadd! vim-sleuth
    packadd! vim-scriptease
    packadd! vim-markdown
    packadd! vim-fugitive
    packadd! vim-commentary
    packadd! vim-dispatch
    packadd! vim-projectionist
    packadd! vim-eunuch
    packadd! vim-surround
    packadd! vim-rhubarb
    packadd! vim-repeat
    packadd! vim-clipper
    packadd! loupe
    packadd! terminus
    packadd! pinnacle
    packadd! command-t
    packadd! ferret
    packadd! vim-easydir
    packadd! indentline
    packadd! auto-pairs
    packadd! vim-slime
    packadd! vim-test
    packadd! vim-lion
    packadd! vim-tmux-navigator
    packadd! ultisnips
    packadd! vim-snippets
    packadd! vim-json
    packadd! vimtex
    packadd! vim-textobj-comment
    packadd! vim-textobj-user
    packadd! base16-vim
    if has('nvim')
      packadd! float-preview
      packadd! languageclient-neovim
    endif
    " The following packages are uninitialized git submodules using `git
    " submodule deinit -force -- {path}`. To enable, uncomment the corresponding
    " line, run `git submodule update --init {path}` and restart vim.
    " packadd! vim-zettel
    " packadd! vim-text-lists
    " packadd! vim-fish
    " packadd! vim-tmux-runner
    " packadd! vim-pandoc
    " packadd! vim-pandoc-syntax
    " packadd! tex-conceal
    " packadd! vim-vinegar
    " packadd! vim-grepper
  else
    source $HOME/.vim/pack/bundle/opt/vim-pathogen/autoload/pathogen.vim
    call pathogen#infect('pack/bundle/opt/{}')
  endif
endif

" Must come *after* the `:packadd!` calls above otherwise the contents of
" package "ftdetect" directories won't be evaluated.
filetype indent plugin on
syntax on

let mapleader = "\<Space>"
let maplocalleader="\\"
